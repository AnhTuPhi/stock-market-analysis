name: Fetching Company Info UAT

on:
  push:
    branches: [ "dev" ]
  schedule:
    - cron: 35 11 * * * # 6:35 PM in Vietnam

jobs:
  fetch-data:
    runs-on: ubuntu-24.04
    environment: secrets
    steps:
      - name: checkout repo content
        uses: actions/checkout@v4
        with:
          ref: dev # checkout dev branch
          fetch-depth: 0

      - name: setup python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: install uv
        run: |
          pip install uv

      - name: install dependencies with uv
        run: |
          uv sync --frozen

      - name: fetch company info
        run: |
          cd src
          uv run python company-info.py

      - name: get current date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: push changes
        uses: actions-x/commit@v6
        with:
          token: ${{ secrets.MY_GITHUB_TOKEN }}
          message: Updated company overview at ${{ env.date }}
          email: action@github.com
          name: GitHub Action
